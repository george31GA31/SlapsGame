// 1. Select the "Play Bots" button
const playBotBtn = document.getElementById('play-bot-btn');

// 2. Add an event listener to check for clicks
playBotBtn.addEventListener('click', () => {
    // 3. For now, just show a message so we know it works
    alert("System Check: Ready to launch Bot Mode!");
    
    // In the next section, we will make this hide the menu 
    // and start the actual game logic.
});
